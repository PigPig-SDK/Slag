<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="OpenglAvaloniaTest.ViewModels.HierarchyView">
        <Grid RowDefinitions="Auto,Auto,*">
            <Button Grid.Row="0" Content="AddObject" CornerRadius="0" HorizontalAlignment="Stretch" Focusable="False" Click="OnAddObjectButtonClick">
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Cube" Click="OnAddCubePressed"/>
                        <MenuItem Header="Sphere" Click="OnAddSpherePressed"/>
                        <MenuItem Header="Cylinder" Click="OnAddCylinderPressed"/>
                        <MenuItem Header="Cone" Click="OnAddConePressed"/>
                        <MenuItem Header="Torus" Click="OnAddTorusPressed"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <Separator Grid.Row="1"/>
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="HierarchyStack"/>
            </ScrollViewer>
        </Grid>
</UserControl>
